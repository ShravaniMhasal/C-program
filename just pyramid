#include <stdio.h>

int main() 
0 {
    int x, i, j;

    printf("Enter the number of x: ");
    scanf("%d", &x);
    for (i = 1; i <= x; ++i) 
    {
        
        for (j = 1; j <= i; ++j) 
        {
            printf("*");
        }
        printf("\n"); 
    }
return 0;
}
